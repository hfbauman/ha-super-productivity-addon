version: '3.3'

services:
  webdav:
    image: bytemark/webdav
    container_name: webdav
    restart: unless-stopped
    ports:
      - "80:80"
    environment:
      - AUTH_TYPE=Basic
      - USERNAME=admin
      - PASSWORD=password
    volumes:
      - ./data:/var/lib/dav
